<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>
    <groupId>com.scriptum</groupId>
    <artifactId>backend</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <name>backend</name>
    <description>Scriptum Backend with JWT, PostgreSQL and Email Verification</description>

    <url/>
    <licenses>
        <license/>
    </licenses>
    <developers>
        <developer/>
    </developers>
    <scm>
        <connection/>
        <developerConnection/>
        <tag/>
        <url/>
    </scm>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.4.5</version>
        <relativePath/>
    </parent>

    <properties>
        <java.version>21</java.version>
        <jjwt.version>0.12.3</jjwt.version>
        <compiler-plugin.version>3.13.0</compiler-plugin.version>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>
        <maven.compiler.proc>full</maven.compiler.proc>
        <lombok.version>1.18.38</lombok.version>
        <jacoco-plugin.version>0.8.13</jacoco-plugin.version>
        <!--
            Set just under what the suite actually achieves, so the gate catches a
            regression rather than merely permitting the status quo. Raise these when
            coverage rises; never lower them to make a red build go green.
        -->
        <jacoco.line.minimum>0.95</jacoco.line.minimum>
        <jacoco.branch.minimum>0.80</jacoco.branch.minimum>

        <!--
            The three test layers, in the order the Maven lifecycle runs them:
            unit tests (*Test) under Surefire in `test`, then integration tests
            (*IT) and system tests (*SystemIT) under Failsafe in
            `integration-test`. Each can be skipped on its own so a pipeline can
            report one layer per step without re-running the others.
        -->
        <skipUnitTests>false</skipUnitTests>
        <skipIntegrationTests>false</skipIntegrationTests>

        <!--
            System tests are off by default and turned on by the `system-tests`
            profile. They boot Tomcat on a real port and are the slowest layer,
            so `mvn verify` stays usable during development and the pipeline
            stage that runs them is the only thing that pays for them.
        -->
        <skipSystemTests>true</skipSystemTests>

        <!-- Set by jacoco:prepare-agent-integration; empty when jacoco is skipped. -->
        <failsafeArgLine/>
    </properties>

    <dependencies>
        <!-- Spring Boot Web -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- Spring Boot Security -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>

        <!-- Spring Boot Data JPA -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <!-- PostgreSQL Driver -->
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- Spring Mail -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-mail</artifactId>
        </dependency>

        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>${lombok.version}</version>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt</artifactId>
            <version>${jjwt.version}</version>
        </dependency>

        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>2.8.6</version>
        </dependency>
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-api</artifactId>
            <version>2.8.6</version>
        </dependency>

        <!--
            Bean Validation. The starter is required, not just jakarta.validation-api:
            the API jar alone leaves no validation provider on the classpath, so
            ValidationAutoConfiguration backs off and every @Valid on a controller
            silently does nothing.
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- ==================== Test ==================== -->

        <!-- JUnit 5, Mockito, AssertJ, MockMvc, JsonPath -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- @WithMockUser and SecurityMockMvcRequestPostProcessors -->
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- In-memory database for @DataJpaTest, PostgreSQL compatibility mode -->
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${compiler-plugin.version}</version>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                    <release>${java.version}</release>
                    <compilerArgs>
                        <arg>-parameters</arg>
                    </compilerArgs>
                    <forceJavacCompilerUse>true</forceJavacCompilerUse>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
            <!-- Unit tests: *Test, no Spring context or a mocked one, always fast. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <skipTests>${skipUnitTests}</skipTests>
                    <includes>
                        <include>**/*Test.java</include>
                    </includes>
                    <excludes>
                        <!-- A composed annotation, not a test class. -->
                        <exclude>**/WebLayerTest.java</exclude>
                    </excludes>
                </configuration>
            </plugin>

            <!--
                Integration and system tests, both under Failsafe in the
                `integration-test` phase. Two executions rather than one, so a
                pipeline can run and report them separately, and so the slower
                system tests only start once integration has passed. Failsafe
                executions run in declaration order.
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <configuration>
                    <argLine>${failsafeArgLine}</argLine>
                </configuration>
                <executions>
                    <!--
                        spring-boot-starter-parent already binds an execution
                        called `default` with Failsafe's own includes. Left
                        alone, it runs every IT a second time before the two
                        executions below.
                    -->
                    <execution>
                        <id>default</id>
                        <phase>none</phase>
                    </execution>
                    <execution>
                        <id>integration-tests</id>
                        <goals>
                            <goal>integration-test</goal>
                        </goals>
                        <configuration>
                            <skipTests>${skipIntegrationTests}</skipTests>
                            <includes>
                                <include>**/*IT.java</include>
                            </includes>
                            <excludes>
                                <exclude>**/*SystemIT.java</exclude>
                            </excludes>
                        </configuration>
                    </execution>
                    <execution>
                        <id>system-tests</id>
                        <goals>
                            <goal>integration-test</goal>
                        </goals>
                        <configuration>
                            <skipTests>${skipSystemTests}</skipTests>
                            <includes>
                                <include>**/*SystemIT.java</include>
                            </includes>
                        </configuration>
                    </execution>
                    <!--
                        Failsafe records failures instead of throwing, so that
                        post-integration-test cleanup always runs. This is the
                        goal that turns a recorded failure into a failed build.
                    -->
                    <execution>
                        <id>verify</id>
                        <goals>
                            <goal>verify</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>${jacoco-plugin.version}</version>
                <configuration>
                    <!--
                        Excluded from coverage: Lombok-generated POJOs (DTOs, domain records,
                        JPA entities, exception detail builders), the application bootstrap class,
                        and AuthService (dead code: no references, AuthController inlines the logic).
                        Measuring generated getters/builders inflates the denominator without
                        saying anything about tested behaviour.
                    -->
                    <excludes>
                        <exclude>com/scriptum/backend/domain/request/**</exclude>
                        <exclude>com/scriptum/backend/domain/response/**</exclude>
                        <exclude>com/scriptum/backend/domain/entities/**</exclude>
                        <exclude>com/scriptum/backend/infrastructure/database/jpa/**</exclude>
                        <exclude>com/scriptum/backend/configuration/exception/**</exclude>
                        <exclude>com/scriptum/backend/ScriptumBackendApplication.class</exclude>
                        <exclude>com/scriptum/backend/service/AuthService.class</exclude>
                    </excludes>
                </configuration>
                <executions>
                    <!--
                        Two agents writing two files: Surefire's run and
                        Failsafe's. They are merged before the report, because
                        coverage of a class is the union of what every layer
                        exercised, and half the integration coverage would
                        otherwise vanish from the gate below.
                    -->
                    <execution>
                        <id>jacoco-prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                        <configuration>
                            <destFile>${project.build.directory}/jacoco-unit.exec</destFile>
                        </configuration>
                    </execution>
                    <execution>
                        <id>jacoco-prepare-agent-integration</id>
                        <goals>
                            <goal>prepare-agent-integration</goal>
                        </goals>
                        <configuration>
                            <destFile>${project.build.directory}/jacoco-it.exec</destFile>
                        </configuration>
                    </execution>
                    <execution>
                        <id>jacoco-merge</id>
                        <phase>post-integration-test</phase>
                        <goals>
                            <goal>merge</goal>
                        </goals>
                        <configuration>
                            <fileSets>
                                <fileSet>
                                    <directory>${project.build.directory}</directory>
                                    <includes>
                                        <include>jacoco-unit.exec</include>
                                        <include>jacoco-it.exec</include>
                                    </includes>
                                </fileSet>
                            </fileSets>
                            <destFile>${project.build.directory}/jacoco.exec</destFile>
                        </configuration>
                    </execution>
                    <execution>
                        <id>jacoco-report</id>
                        <phase>post-integration-test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                        <configuration>
                            <dataFile>${project.build.directory}/jacoco.exec</dataFile>
                        </configuration>
                    </execution>
                    <execution>
                        <id>jacoco-check</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                        <configuration>
                            <dataFile>${project.build.directory}/jacoco.exec</dataFile>
                            <rules>
                                <rule>
                                    <element>BUNDLE</element>
                                    <limits>
                                        <limit>
                                            <counter>LINE</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>${jacoco.line.minimum}</minimum>
                                        </limit>
                                        <limit>
                                            <counter>BRANCH</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>${jacoco.branch.minimum}</minimum>
                                        </limit>
                                    </limits>
                                </rule>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!--
            Adds the system test layer on top of the others:
            `./mvnw -Psystem-tests verify` runs unit, integration and system.
            Used by the system stage of the CI pipeline.
        -->
        <profile>
            <id>system-tests</id>
            <properties>
                <skipSystemTests>false</skipSystemTests>
            </properties>
        </profile>
    </profiles>

</project>
